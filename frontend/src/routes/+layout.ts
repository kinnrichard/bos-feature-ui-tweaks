// Disable SSR for Zero compatibility
export const ssr = false;

// Enable client-side rendering for real-time features
export const csr = true;

// Prerender is not compatible with Zero's real-time features
export const prerender = false;